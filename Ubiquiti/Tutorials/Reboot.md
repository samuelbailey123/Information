## How to reboot any Unifi device ## 

simply ssh into the IP of the device:
- SSH into your Ubuntu/Debian machine, and login as root. ( Ubuntu | sudo -i | Debian | su )
then run the following command:
```sudo reboot```
